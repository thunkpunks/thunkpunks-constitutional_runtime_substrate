# Constitutional Receipt Demonstration — Status

## Name

**The Constitutional Receipt Demonstration** — the canonical proof artifact that
the constitutional separation produces operational leverage, not just coherence.

File: `runtime/demos/regulated_receipt_demo.py`
Tests: `runtime/tests/test_regulated_receipt_demo.py` (15/15)

## Status: BOUNDED (canonical, frozen)

This is the stabilized proof surface. It is complete for what it claims and is
deliberately NOT extended (no third face). It is the reference demonstration; if
it regresses, the leverage claim regresses.

## What it proves

The same evidential circuit — kernel decides, substrate remembers, observability
watches — produces DISCRIMINATING, PROVABLE, TAMPER-EVIDENT decision records that
an ordinary orchestration stack cannot natively produce.

Paired proof surface, one circuit, two fictional clinical workflows:

| Workflow (fictional) | Runtime situation | Kernel verdict | Reasons |
|----------------------|-------------------|----------------|---------|
| decision-support summary (read-only) | high optionality, reversible, low residue | **EXECUTE** | admissible_as_proposed |
| write-back to patient record | high commitment, low renegotiability, irrecoverable residue | **DEFER** | tau_at_ceiling_without_hbtl, hbtl_trigger, theta_below_floor |

Both receipts: genuine (real `run_loop` over the real gate), verified by the
auditor view, and fail closed the instant any link (receipt payload, bound
event, lineage) is altered.

## Why the pairing is the proof (not either alone)

- A lone DEFER reads as "merely conservative."
- A lone EXECUTE reads as "rubber stamp."
- The PAIR shows the separation DISCRIMINATES: yes to the low-stakes step, hold
  the high-stakes one, on constitutional grounds the kernel derived from the
  runtime situation — each provable and tamper-evident.

## The auditor view

Read-only, observability semantics: it reads snapshots, reports decision +
reasons + per-link integrity + a non-authoritative trace observation, and
influences nothing. The thing that shows the decision is the watcher, not the
decider.

## Discipline held (per continuation contract)

- no new authority — runs the existing circuit; added one demo file + tests
- no orchestration expansion
- no scoring/action loop — the view reads and reports; acts on nothing
- no synthetic receipts — genuine `issue_receipt` products; determinism-tested
- no fabricated provenance — workflow labelled fiction; evidence real

## Frozen scope

NOT built (recorded, not built):
- **third face: institutional decision lineage** — showing a SEQUENCE of these
  decisions forms a provable derivation chain (a write-back DEFER descended from
  a prior support EXECUTE). A thin read of substrate already built. Deferred
  deliberately; two faces suffice to make the threshold point.

## Classification

Layer: OBSERVABILITY (read-only presentation coordinator). Non-authoritative.
Adds no decision capability; reads and displays genuine circuit output.

## Build order position

  ... -> closed circuit (frozen) -> read-only observability -> **Constitutional
  Receipt Demonstration (canonical, bounded here)**.

The proof surface is consolidated. Soundness was the prior phase; this is the
first demonstrated leverage, frozen as the reference artifact.
